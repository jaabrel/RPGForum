@page "{id:int}"
@model RPGForum.Pages.Admin.Personagens.EditModel
@{
    ViewData["Title"] = "Editar Personagem";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-dark text-white">
                    <h3 class="mb-0 h5">⚙️ Editar Personagem: @Model.Personagem.Name</h3>
                </div>
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="Personagem.Id" />

                        <div class="mb-3">
                            <label asp-for="Personagem.Name" class="form-label fw-bold">Nome da Personagem</label>
                            <input asp-for="Personagem.Name" class="form-control" />
                            <span asp-validation-for="Personagem.Name" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Personagem.Description" class="form-label fw-bold">Descrição</label>
                            <textarea asp-for="Personagem.Description" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Personagem.Description" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Personagem.ImageUrl" class="form-label fw-bold">URL da Imagem</label>
                            <input asp-for="Personagem.ImageUrl" class="form-control" />
                            <span asp-validation-for="Personagem.ImageUrl" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-page="./Index" class="btn btn-outline-secondary">Cancelar</a>
                            <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
